<controls:MetroWindow x:Class="JD_XI_Editor.Views.MainWindowView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      mc:Ignorable="d"
                      ShowTitleBar="False">
    <controls:MetroWindow.RightWindowCommands>
        <controls:WindowCommands>
            <StackPanel Orientation="Horizontal">
                <Label Content="Input Device" />
                <ComboBox Width="150" 
                          ItemsSource="{Binding InputDevices}"
                          DisplayMemberPath="Name" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Output Device" />
                <ComboBox Width="150"
                          ItemsSource="{Binding OutputDevices}" 
                          DisplayMemberPath="Name" />
            </StackPanel>
        </controls:WindowCommands>
    </controls:MetroWindow.RightWindowCommands>
    <Grid>
        <TabControl>
            <TabItem Header="Digital Synth 1" />
            <TabItem Header="Digital Synth 2" />
            <TabItem Header="Drums" />
            <TabItem Header="Analog Synth" />
        </TabControl>
    </Grid>
</controls:MetroWindow>



